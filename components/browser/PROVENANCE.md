# Source provenance

This browser candidate started from selected current bytes in `/home/mainpc/Downloads/pi-web-workspace` on 2026-08-12.

The reference declares Apache-2.0 in its root `LICENSE`. Its original Git identity is unavailable. It was not a Git repository at intake. Its historical `MANIFEST.sha256` was stale: 31 of 153 paths differed. The current source-path hashes from the foundation inventory are preserved at `provenance/reference/current-source-manifest.sha256`. The historical manifest and controlling reference instructions are also preserved under `provenance/reference/`.

The initial import excludes reference validation logs and generated dependency/build directories. Current code is reviewed as owned code. Preserve Apache-2.0 notices for adapted code. Track other upstream licenses in `docs/upstream.md` and final release notices.
