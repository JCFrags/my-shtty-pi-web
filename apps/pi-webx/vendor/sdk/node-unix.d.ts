import type { NdjsonConnectionFactory } from "./transport.js";
/** Create one real same-user Unix NDJSON connection for WebX transport use. */
export declare const nodeNdjsonConnectionFactory: NdjsonConnectionFactory;
